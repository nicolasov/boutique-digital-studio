---
import { translations } from '../i18n/translations';

const services = [
  ['serviceWebTitle', 'serviceWebText'],
  ['serviceBrandTitle', 'serviceBrandText'],
  ['serviceAiTitle', 'serviceAiText'],
  ['serviceContentTitle', 'serviceContentText'],
] as const;

const t = translations.es;
---

<section id="soluciones-digitales" class="services-section section-pad">
  <div class="section-shell">
    {t.servicesIntro && (
      <div class="services-intro scroll-reveal">
        <p class="services-intro-statement display-font" data-i18n="servicesIntro">
          {t.servicesIntro}
        </p>
      </div>
    )}

    <div class="services-accordion">
      {services.map(([title, text]) => (
        <details class="solution-item group">
          <summary class="solution-summary">
            <h3 class="solution-title display-font" data-i18n={title}>
              {t[title]}
            </h3>
            <span class="solution-toggle" aria-hidden="true"></span>
          </summary>
          <div class="solution-body" data-solution-body>
            <p class="solution-copy">
              <span data-i18n={text}>{t[text]}</span>
            </p>
          </div>
        </details>
      ))}
    </div>
  </div>
</section>
