---
import { translations } from '../i18n/translations';
import WebGLPixelImage from './WebGLPixelImage.astro';

const services = [
  {
    titleLine1: 'Sitios web',
    titleLine2: 'premium',
    text: translations.es.workOneText,
    image: '/assets/service-web.png',
  },
  {
    titleLine1: 'Identidad',
    titleLine2: 'de marca',
    text: translations.es.workTwoText,
    image: '/assets/service-branding.png',
  },
  {
    titleLine1: 'Automatización e',
    titleLine2: 'inteligencia artificial',
    text: translations.es.serviceAiWhat,
    image: '/assets/service-ai.png',
  },
  {
    titleLine1: 'Contenido',
    titleLine2: 'visual',
    text: translations.es.workFourText,
    image: '/assets/service-contenido.png',
  },
] as const;
---

<section id="soluciones-digitales" class="services-section section-pad">
  <div class="section-shell">
    <div class="services-accordion">
      {services.map((service, index) => (
        <details class="solution-item group" name="services-editorial" open={index === 0}>
          <summary class="solution-summary">
            <h3 class="solution-title display-font">
              {service.titleLine1}<br />{service.titleLine2}
            </h3>
            <span class="solution-toggle" aria-hidden="true"></span>
          </summary>
          <div class="solution-body" data-solution-body>
            <div class="solution-copy-wrap">
              <p class="solution-copy">{service.text}</p>
              <span class="solution-indicator" aria-hidden="true"></span>
            </div>
            <WebGLPixelImage
              class="solution-image service-image-wrap"
              src={service.image}
              alt={`Imagen editorial para ${service.titleLine1} ${service.titleLine2}`}
              intensity={0.32}
              speed={0.74}
              scale={1.01}
              triggerOnce={false}
            />
          </div>
        </details>
      ))}
    </div>
  </div>
</section>
